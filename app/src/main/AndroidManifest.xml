<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.happy.happylists" >

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        >

        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="NewSpisok"></activity>
        <activity android:name="SpisPokyp"></activity>
        <activity android:name="MainSettigs"></activity>
        <activity android:name="Dictionaries"></activity>
        <activity android:name="DicKateg"></activity>
        <activity android:name="DicProd"></activity>
        <activity android:name="DicEdin"></activity>

    </application>

</manifest>
